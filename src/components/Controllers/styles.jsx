import styled from "styled-components";
import { colors } from "../UI/colors";

export const DivControllers = styled.div`
    display: flex;
    justify-content: space-between;
    font-size: 2.8rem;
    color: ${colors["gray-light-controllers"]}

`